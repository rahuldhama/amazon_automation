# amazon_automation
internship task
# Amazon Automation Assessment
Built using Python and Playwright.

## Setup
1. Clone this repository.
2. Create a virtual environment: `python -m venv venv`
3. Activate: `.\venv\Scripts\activate`
4. Install: `pip install -r requirements.txt` and `playwright install chromium`

## Run Tests in Parallel
`python -m pytest -n 2 --headed -s`
